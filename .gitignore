# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Temporary files, for example, from tests.
/tmp/

# Ignore package tarball (built via "mix hex.build").
onestack-*.tar

# Ignore assets that are produced by build tools.
/priv/static/assets/

# Ignore digested assets cache.
/priv/static/cache_manifest.json

# In case you use Node.js/npm, you want to ignore these.
npm-debug.log
/assets/node_modules/

# Database files
*.db
*.db-*

node_modules
.yarn
.husky
example-apps
apps
packages
.env*
!.env.example
onestack_products/**/.env
.DS_Store

.dev.env
.prod.env

# SQLite databases (with and without .db extension)
onestack_dev
onestack_dev-shm
onestack_dev-wal
onestack_test
onestack_test-shm
onestack_test-wal

# Traefik certificates
**/acme.json

# Plane env (not matched by **/.env pattern)
onestack_products/plane_docker/plane.env

# Binary archives
*.zip

# Loose scripts at root
/restore_subscriptions.exs

# Vault password - NEVER commit this
.vault-password
vault-password
*.vault-password
